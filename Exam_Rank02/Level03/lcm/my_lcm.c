#include <unistd.h>
#include <stdio.h>

unsigned int lcm(unsigned int a, unsigned int b)
{
    unsigned int c;
    


    return (c);
}

int main(void)
{
    unsigned int a = 5;
    unsigned int b = 15;
    unsigned int c = lcm(a, b);

    printf("The LCM of %d and %d is: %d\n", a, b, c);
    return (0);
}